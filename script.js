import * as THREE from "https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js";

const reducedMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;
const revealElements=document.querySelectorAll(".reveal");
if(reducedMotion)revealElements.forEach(el=>el.classList.add("show"));else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("show");observer.unobserve(entry.target)}}),{threshold:.1});revealElements.forEach(el=>observer.observe(el))}
function setupRenderer(canvas){const r=new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});r.setPixelRatio(Math.min(devicePixelRatio,2));r.outputColorSpace=THREE.SRGBColorSpace;r.toneMapping=THREE.ACESFilmicToneMapping;r.toneMappingExposure=1.45;return r}
const canvas=document.querySelector("#three-canvas"),holder=document.querySelector(".hero-visual"),status=document.querySelector("#three-status");
try{const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(34,1,.1,100);camera.position.set(0,.05,9.3);const renderer=setupRenderer(canvas),world=new THREE.Group();world.scale.setScalar(.82);world.rotation.z=-.12;scene.add(world);const planet=new THREE.Mesh(new THREE.SphereGeometry(1.72,128,128),new THREE.MeshPhysicalMaterial({color:0xaaa9b5,metalness:.82,roughness:.08,clearcoat:1,clearcoatRoughness:.025,iridescence:1,iridescenceIOR:1.75,iridescenceThicknessRange:[180,1000]}));world.add(planet);const ringMat=new THREE.MeshPhysicalMaterial({color:0xc4c0d3,metalness:.88,roughness:.07,clearcoat:1,transparent:true,opacity:.93,side:THREE.DoubleSide,iridescence:1,iridescenceIOR:1.8,iridescenceThicknessRange:[160,1100]});const ring=new THREE.Mesh(new THREE.TorusGeometry(2.36,.17,40,240),ringMat);ring.rotation.set(1.16,.18,.04);world.add(ring);const upperRing=new THREE.Mesh(new THREE.TorusGeometry(2.5,.065,24,220),ringMat.clone());upperRing.material.opacity=.72;upperRing.rotation.set(.66,1.04,-.24);world.add(upperRing);const orbit=new THREE.Mesh(new THREE.TorusGeometry(3.05,.012,8,240),new THREE.MeshBasicMaterial({color:0xb5b7c4,transparent:true,opacity:.36}));orbit.rotation.set(1.26,.04,-.22);world.add(orbit);const moonPivot=new THREE.Group();world.add(moonPivot);const moon=new THREE.Mesh(new THREE.SphereGeometry(.14,40,40),new THREE.MeshPhysicalMaterial({color:0xe3e0e8,metalness:.65,roughness:.08,clearcoat:1,iridescence:.7}));moon.position.set(3.05,.05,0);moonPivot.add(moon);const sg=new THREE.BufferGeometry(),count=800,pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*24;pos[i*3+1]=(Math.random()-.5)*15;pos[i*3+2]=-3-Math.random()*14}sg.setAttribute("position",new THREE.BufferAttribute(pos,3));const stars=new THREE.Points(sg,new THREE.PointsMaterial({color:0xe1e5f5,size:.022,transparent:true,opacity:.82}));scene.add(stars);scene.add(new THREE.HemisphereLight(0xcbd3ef,0x07080c,1.8));[[0xffffff,64,3.8,3.6,5.4],[0x8e82ff,46,-3.8,-1.4,4.2],[0xffead8,36,2.7,-3.2,3.4],[0x8fb8ff,26,-2.2,3,2.5]].forEach(a=>{const l=new THREE.PointLight(a[0],a[1],28);l.position.set(a[2],a[3],a[4]);scene.add(l)});let px=0,py=0,dx=0,dy=0,drag=false,lx=0,ly=0;holder.addEventListener("pointermove",e=>{const r=holder.getBoundingClientRect();px=((e.clientX-r.left)/r.width-.5)*2;py=((e.clientY-r.top)/r.height-.5)*2;if(drag){dy+=(e.clientX-lx)*.007;dx+=(e.clientY-ly)*.007;lx=e.clientX;ly=e.clientY}});holder.addEventListener("pointerdown",e=>{drag=true;lx=e.clientX;ly=e.clientY});holder.addEventListener("pointerup",()=>drag=false);holder.addEventListener("pointerleave",()=>{drag=false;px=py=0});function resize(){const w=Math.max(holder.clientWidth,1),h=Math.max(holder.clientHeight,1);renderer.setSize(w,h,false);camera.aspect=w/h;camera.fov=w<520?42:34;camera.position.z=w<520?10.3:9.3;world.scale.setScalar(w<520?.7:w<760?.76:.82);camera.updateProjectionMatrix()}resize();addEventListener("resize",resize);const clock=new THREE.Clock();function animate(){const t=clock.getElapsedTime();if(!reducedMotion){planet.rotation.y=t*.1;ring.rotation.z=.04+t*.055;upperRing.rotation.z=-.24-t*.038;orbit.rotation.z=-.22+t*.018;moonPivot.rotation.z=t*.25;stars.rotation.y=t*.0025;world.rotation.y+=((px*.16+dy)-world.rotation.y)*.03;world.rotation.x+=((-py*.1+dx)-world.rotation.x)*.03;world.position.y=Math.sin(t*.55)*.055}renderer.render(scene,camera);requestAnimationFrame(animate)}animate();status.textContent="DRAG / MOVE"}catch(e){console.error(e);status.textContent="3D VIEW UNAVAILABLE"}

const starCanvas=document.querySelector("#star-canvas"),starHolder=document.querySelector(".about-visual");
try{const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(38,1,.1,50);camera.position.set(0,0,8.6);const renderer=setupRenderer(starCanvas),group=new THREE.Group();group.scale.setScalar(.78);scene.add(group);const shape=new THREE.OctahedronGeometry(1.32,0);const p=shape.attributes.position;for(let i=0;i<p.count;i++){const x=p.getX(i),y=p.getY(i),z=p.getZ(i);p.setXYZ(i,x*(Math.abs(x)>.65?1.42:.82),y*(Math.abs(y)>.65?1.48:.85),z*(Math.abs(z)>.65?1.22:.8))}p.needsUpdate=true;shape.computeVertexNormals();const crystal=new THREE.Mesh(shape,new THREE.MeshPhysicalMaterial({color:0xc9c5d6,metalness:.82,roughness:.055,clearcoat:1,clearcoatRoughness:.015,iridescence:1,iridescenceIOR:1.85,iridescenceThicknessRange:[130,1200],flatShading:true}));crystal.rotation.z=Math.PI/4;group.add(crystal);const orbit=new THREE.Mesh(new THREE.TorusGeometry(2.28,.012,8,180),new THREE.MeshBasicMaterial({color:0xc4c7d4,transparent:true,opacity:.58}));orbit.rotation.set(1.12,.22,.08);group.add(orbit);const pivot=new THREE.Group();pivot.rotation.copy(orbit.rotation);group.add(pivot);const moon=new THREE.Mesh(new THREE.SphereGeometry(.095,24,24),new THREE.MeshPhysicalMaterial({color:0xe9e6ef,metalness:.72,roughness:.06,clearcoat:1}));moon.position.set(2.28,0,0);pivot.add(moon);scene.add(new THREE.HemisphereLight(0xe2e7ff,0x090a0e,2.2));[[0xffffff,50,3,3,4],[0x9a88ff,34,-3,-1,3],[0xffdfc8,28,2,-2,2]].forEach(a=>{const l=new THREE.PointLight(a[0],a[1],20);l.position.set(a[2],a[3],a[4]);scene.add(l)});function resizeStar(){const w=Math.max(starHolder.clientWidth,1),h=Math.max(starHolder.clientHeight,1);renderer.setSize(w,h,false);camera.aspect=w/h;camera.position.z=w<420?10:8.6;group.scale.setScalar(w<420?.64:.78);camera.updateProjectionMatrix()}resizeStar();addEventListener("resize",resizeStar);const clock=new THREE.Clock();function animateStar(){const t=clock.getElapsedTime();if(!reducedMotion){crystal.rotation.y=t*.2;crystal.rotation.x=Math.sin(t*.42)*.1;crystal.rotation.z=Math.PI/4;pivot.rotation.z=.08+t*.28;group.position.y=Math.sin(t*.62)*.035}renderer.render(scene,camera);requestAnimationFrame(animateStar)}animateStar()}catch(e){console.error("About star:",e)}